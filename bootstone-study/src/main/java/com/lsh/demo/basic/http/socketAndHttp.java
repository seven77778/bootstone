package com.lsh.demo.basic.http;

import org.junit.Test;

/**
 * Created by lsh on 2019-09-06.
 *
 * http 和 socket 的区别
 *
 * 【http】 所谓的短连接
 *
 *
 * 【socket】 长连接
 * socket 的原意是“插座”，在计算机通信领域，socket 被翻译为“套接字”，它是计算机之间进行通信的一种约定或一种方式。
 * 通过 socket 这种约定，一台计算机可以接收其他计算机的数据，也可以向其他计算机发送数据
 *
 * 【socket使用场景】
 * 1.门锁服务的 客户端通过socket调用服务端
 *
 */
public class socketAndHttp {

    @Test
    public void test(){
        for (int i = 0; i < ; i++) {
            
        }
    }
}
